# Pin the toolchain for reproducible CI and developer builds.
# Bump intentionally; keep in sync with .github/workflows/ci.yml as needed.
[toolchain]
channel = "1.85.0"
components = ["rustfmt", "clippy"]
profile = "minimal"
